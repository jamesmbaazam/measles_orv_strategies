#####################################
#inputs
######################################
sc_model_param_set <- list(ambient_temp = c("below 40", "above 40")
                , mf314_quant = 1
                , buffer_stock = 0 #no buffer for now 
                , near_pop = 1500 
                , far_pop = 500
                , site_team_alloc = 1 #number of teams allocated to site
                )
